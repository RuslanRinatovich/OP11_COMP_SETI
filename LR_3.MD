# Лабораторная работа №3  
**Тема:** Диагностика сетевого соединения с помощью утилит TCP/IP  

## Оглавление
- [Цели работы](#цели-работы)  
- [Теоретическая справка (для записи в тетрадь)](#теоретическая-справка-для-записи-в-тетрадь)  
  - [`ipconfig` — просмотр и управление сетевой конфигурацией](#1-ipconfig--просмотр-и-управление-сетевой-конфигурацией)  
  - [`ping` — проверка доступности удалённого узла](#2-ping--проверка-доступности-удалённого-узла)  
  - [`tracert` (trace route) — трассировка маршрута до узла](#3-tracert-trace-route--трассировка-маршрута-до-узла)  
  - [`netstat` — анализ сетевых подключений и статистики](#4-netstat--анализ-сетевых-подключений-и-статистики)  
  - [`arp` — работа с ARP-таблицей](#5-arp--работа-с-arp-таблицей)  
  - [`hostname` — отображение имени компьютера](#6-hostname--отображение-имени-компьютера)  
- [Практическая часть](#практическая-часть)  
  - [Подготовка](#подготовка)  
  - [Задания](#задания)  
- [Контрольные вопросы (включите ответы в отчёт)](#контрольные-вопросы-включите-ответы-в-отчёт)  
- [Требования к оформлению отчёта](#требования-к-оформлению-отчёта)  
- [Критерии оценки](#критерии-оценки)  

---

## Цели работы
- Освоить работу с основными утилитами диагностики TCP/IP в командной строке Windows.  
- Научиться анализировать сетевую доступность, маршрутизацию и состояние соединений.  
- Получить навыки оформления технического отчёта.

---

## Теоретическая справка (для записи в тетрадь)

В состав большинства операционных систем (включая Windows) входят встроенные утилиты командной строки для диагностики и настройки сетевого стека TCP/IP. Эти инструменты позволяют системному администратору проверять работоспособность сети, выявлять неисправности и анализировать сетевой трафик.

Ниже приведены основные утилиты и их назначение:

### 1. `ipconfig` — просмотр и управление сетевой конфигурацией
- **Назначение**: отображение текущих параметров TCP/IP (IP-адрес, маска подсети, шлюз) и управление DHCP/DNS-настройками.  
- **Основные ключи**:
  - `ipconfig` — базовая информация.  
  - `/all` — полная конфигурация (включая MAC-адрес, DHCP, DNS-серверы).  
  - `/release` — освободить IP-адрес (при использовании DHCP).  
  - `/renew` — запросить новый IP-адрес.  
  - `/flushdns` — очистить кэш DNS.  
  - `/displaydns` — показать содержимое DNS-кэша.


### ⚠️ 1.1 в Astra Linux аналогом команды ipconfig является **`ip a`**

# `ip a` — просмотр и управление сетевыми интерфейсами

**Назначение:** отображение и управление сетевыми интерфейсами, IP-адресами, MAC-адресами и состоянием сетевых подключений.

**Основные команды и опции:**

- `ip a` — базовая информация о всех сетевых интерфейсах
- `ip a show [интерфейс]` — подробная информация о конкретном интерфейсе
- `ip a add [адрес/маска] dev [интерфейс]` — добавить IP-адрес к интерфейсу
- `ip a del [адрес/маска] dev [интерфейс]` — удалить IP-адрес с интерфейса
- `ip -4 a` — показать только IPv4 адреса
- `ip -s a` — показать статистику по интерфейсам

**Связанные команды для управления сетью:**
- `ip route` — просмотр и управление таблицей маршрутизации (аналог шлюза)
- `systemctl restart NetworkManager` — перезапуск сетевых настроек (аналог /renew)
- `resolvectl flush-caches` — очистка DNS-кэша (аналог /flushdns)
- `resolvectl statistics` — статистика DNS (аналог /displaydns)


### 2. `ping` — проверка доступности удалённого узла
- **Назначение**: отправка ICMP-эхо-запросов для проверки связи с другим устройством.  
- **Принцип работы**: если узел отвечает, он доступен на сетевом уровне.  
- **Основные ключи**:
  - `-t` — непрерывный пинг (до нажатия Ctrl+C).  
  - `-a` — определение имени узла по IP-адресу.  
  - `-l <размер>` — задание размера пакета (в байтах).  
  - `-n <число>` — количество отправляемых пакетов.  
  - `-w <мс>` — таймаут ожидания ответа.

> ⚠️ Отсутствие ответа не всегда означает недоступность узла — ответ может блокироваться брандмауэром.

### 3. `tracert` (trace route) — трассировка маршрута до узла
- **Назначение**: определение пути, по которому пакеты проходят от источника к цели, с отображением всех промежуточных маршрутизаторов.  
- **Принцип работы**: использует увеличивающееся значение TTL (Time To Live) в IP-заголовке.  
- **Основные ключи**:
  - `-h <макс. хопов>` — ограничение числа промежуточных узлов.  
  - `-w <мс>` — таймаут ожидания ответа от каждого узла.

### ⚠️ 3.1 в Astra Linux аналогом команды tracert является **`traceroute`**
# `traceroute` — трассировка сетевого маршрута

**Назначение:** определение пути следования пакетов от локального компьютера к целевому узлу с отображением всех промежуточных маршрутизаторов.

**Основные опции:**

- `traceroute [хост]` — базовая трассировка до указанного хоста
- `-n` — не разрешать IP-адреса в доменные имена
- `-I` — использовать ICMP-запросы вместо UDP
- `-T` — использовать TCP-запросы
- `-p [порт]` — указать целевой порт
- `-q [число]` — количество пакетов для каждого hop (по умолчанию 3)
- `-m [число]` — максимальное количество hop (по умолчанию 30)
- `-w [секунды]` — время ожидания ответа

### 4. `netstat` — анализ сетевых подключений и статистики
- **Назначение**: отображение активных соединений, прослушиваемых портов и статистики по протоколам.  
- **Основные ключи**:
  - `-a` — все подключения и порты.  
  - `-n` — отображение адресов и портов в числовом виде (без разрешения имён).  
  - `-s` — статистика по протоколам (IP, TCP, UDP, ICMP).  
  - `-e` — статистика по Ethernet-интерфейсу.  
  - `<интервал>` — обновление вывода каждые N секунд.

### 5. `arp` — работа с ARP-таблицей
- **Назначение**: просмотр и редактирование таблицы соответствия IP-адресов и MAC-адресов в локальной сети.  
- **Основные ключи**:
  - `-a` — отобразить всю ARP-таблицу (или запись для конкретного IP).  
  - `-s <IP> <MAC>` — добавить статическую запись.  
  - `-d <IP>` — удалить запись.

### 6. `hostname` — отображение имени компьютера
- **Назначение**: вывод имени текущего хоста в сети.  
- **Использование**: просто введите `hostname` в командной строке.

> 💡 **Совет**: Чтобы получить справку по любой утилите, введите её имя с ключом `/?`, например:  
> ```cmd
> ping /?
> ```

---

## Практическая часть

### Подготовка
1. Откройте **Командную строку** от имени администратора.  
2. Убедитесь, что компьютер подключён к сети (желательно к интернету).

### Задания
Выполните следующие команды и зафиксируйте результаты **скриншотами**:

1. **`ipconfig`**  
   ```cmd
   ipconfig
   ipconfig /all
   ```
   Найдите: IP-адрес, маску, шлюз, MAC-адрес, статус DHCP.

> ⚠️ в Astra Linux аналогом команды ipconfig является **`ip a`**
```cmd
  ip a
```


2. **`ping`**  
   ```cmd
   ping 8.8.8.8
   ping yandex.ru
   ping -l 1000 -n 5 google.com
   ```
> ⚠️ в Astra Linux у команды ping другие параметры. Используйте `ping -s 1000 -c 5 google.com`. (-s размер пакета, -c количество запросов).

3. **`tracert`**  
   ```cmd
   tracert yandex.ru
   ```
> ⚠️ в Astra Linux аналогом команды ipconfig является **`traceroute`**

**`traceroute`**  
   ```cmd
   traceroute yandex.ru
   ```


4. **`netstat`**  
   ```cmd
   netstat -a
   netstat -n
   netstat -s
   ```

5. **`arp`**  
   ```cmd
   arp -a
   ```

6. **`hostname`**  
   ```cmd
   hostname
   ```

## Справка

Сводная таблица аналогов в Astra Linux
|Функция |Команда Windows |Эквивалент в Astra Linux |
|---|---|---|
|Просмотр IP |`ipconfig`|`ip addr show` или `ifconfig`|
|Трассировка маршрута |`tracert`|`traceroute`|
|Сетевая статистика |`netstat`|`ss`  (рекомендуется) или `netstat`|
|Таблица ARP |`arp`|`ip neighbor` или `arp`|
|Имя хоста|`hostname`|`hostname`|


Для каждого задания сделайте **скриншот результата** и кратко опишите:
- Какую команду вы выполняли?
- Что означает полученный вывод?
- Есть ли неожиданные или интересные наблюдения?

---

## Контрольные вопросы (включите ответы в отчёт)

1. Перечислите основные утилиты диагностики TCP/IP. Укажите назначение каждой и 2–3 ключа.  
2. Как узнать физический (MAC) адрес компьютера?  
3. Нужно ли перезагружать компьютер после изменения:  
   - настроек TCP/IP,  
   - имени рабочей группы,  
   - имени компьютера?  
4. Как с помощью `ping` определить, доступен ли удалённый узел? На что смотреть в ответе?  
5. Как узнать IP-адрес по доменному имени (например, `vk.com`)?  
6. Как изменить размер пакета в `ping`? Приведите пример команды.  
7. Компьютер в локальной сети видит все внутренние устройства, но не может выйти в интернет. В чём может быть причина?  
8. Какая утилита выводит имя текущего компьютера?

---

## Требования к оформлению отчёта

Отчёт оформляется **в Microsoft Word (.docx)** и **обязательно сдаётся через Google-форму**.

## 📌 **[Сдать ЛР3. 245 Группа](https://forms.gle/kPnLa7WfcyLEJiLq9)**

## 📌 **[Сдать ЛР3. 247 Группа](https://forms.gle/bHv7LN7aFMaCuoyr5)**

### Обязательные элементы:
- Титульный лист (ФИО, группа, дата, номер и тема работы).  
- Раздел «Теоретическая справка» (можно кратко, но с указанием всех утилит и их ключей).  
- Раздел «Практическая часть» с **скриншотами** и пояснениями к каждому заданию.  
- Ответы на контрольные вопросы.  
- Вывод (3–5 предложений: что узнали, что было сложно, что интересно).

### Форматирование:
- **Шрифт**: Times New Roman  
- **Размер шрифта**: 12 pt  
- **Межстрочный интервал**: 1.5  
- **Поля**: стандартные (2.5 см со всех сторон)  
- **Выравнивание**: по ширине  
- **Подписи к рисункам**: например, *Рис. 1 — Результат выполнения команды ipconfig /all*

> ❗ **Важно**:  
> - Не сдавайте «голый» текст без скриншотов — работа не будет засчитана.  
> - Не вставляйте скриншоты без пояснений.  
> - Не игнорируйте форматирование — неотформатированные отчёты снижают оценку.  
> - Название файла: `Фамилия_ЛР3.docx` (например, `Иванов_ЛР3.docx`).

---

## Критерии оценки

| Оценка | Требования |
|--------|-----------|
| **5** | Полный отчёт с пояснениями и скриншотами всех заданий + ответы на все контрольные вопросы + соблюдено форматирование. |
| **4** | Отчёт с 5–6 заданиями + ответы на 6–7 вопросов + в целом соблюдено форматирование. |
| **3** | Отчёт с 3–4 заданиями + ответы на 4–5 вопросов + частично соблюдено форматирование. |

> 📌 **Напоминание**: Простое копирование теории без скриншотов и анализа — **не засчитывается**.

---
